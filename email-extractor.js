const source=document.querySelector('#email-source'),result=document.querySelector('#email-result'),count=document.querySelector('#email-count');
function extract(){const matches=source.value.match(/[a-z0-9.!#$%&'*+/=?^_`{|}~-]+@[a-z0-9](?:[a-z0-9-]{0,61}[a-z0-9])?(?:\.[a-z0-9](?:[a-z0-9-]{0,61}[a-z0-9])?)+/gi)||[],emails=[...new Set(matches.map(email=>email.toLowerCase()))];count.textContent=`${emails.length} unique email${emails.length===1?'':'s'} found`;result.value=emails.join('\n');}
source.addEventListener('input',extract);document.querySelector('#email-copy').addEventListener('click',async e=>{if(!result.value)return;try{await navigator.clipboard.writeText(result.value)}catch{result.select();document.execCommand('copy')}e.target.textContent='Copied ✓';setTimeout(()=>e.target.textContent='Copy emails',1400)});document.querySelector('#email-clear').addEventListener('click',()=>{source.value='';extract();source.focus()});extract();
